% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{panic_for_test}
\alias{panic_for_test}
\title{Test-only seam for the internal-error path. Panics inside the engine
boundary so testthat can assert the caught panic surfaces as an R error
carrying the report hint (mirrors engine-py's \code{panic_for_test}). Surfaced as
\code{mcpower:::panic_for_test}.}
\usage{
panic_for_test()
}
\description{
Test-only seam for the internal-error path. Panics inside the engine
boundary so testthat can assert the caught panic surfaces as an R error
carrying the report hint (mirrors engine-py's \code{panic_for_test}). Surfaced as
\code{mcpower:::panic_for_test}.
}
