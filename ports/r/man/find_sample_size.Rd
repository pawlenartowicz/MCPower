% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{find_sample_size}
\alias{find_sample_size}
\title{Run a sample-size search (grid) for all scenarios encoded in \code{contracts}.}
\usage{
find_sample_size(
  contracts,
  target_power,
  lo,
  hi,
  n_sims,
  base_seed,
  method,
  by,
  by_kind,
  mode,
  tol_n,
  progress
)
}
\description{
\itemize{
\item \code{target_power}      — desired power threshold (0..1).
\item \code{lo} / \code{hi}         — search bounds (sample sizes).
\item \code{n_sims}            — Monte-Carlo replications per N point.
\item \code{base_seed}         — f64 in R; cast to u64.
\item \code{method}            — \code{"grid"} (the only supported method).
\item \code{by}                — grid step size / point count (required).
\item \code{by_kind}           — \code{"fixed"} (step) or \code{"auto"} (point count); default \code{"fixed"}.
\item \code{mode}              — \code{"linear"} or \code{"log"} (default \code{"linear"}).
\item \code{tol_n}             — accepted for API compatibility; unused.
\item \code{progress}          — an R callback \verb{(current, total)}, or \code{NULL} for
silent. Events are delivered on the R main thread; Ctrl-C / Escape cancels
the run (see \code{progress.rs}).
}
}
\details{
Returns a named list mirroring the Python dict shape:
\verb{list(scenarios = <named list>, comparison = list())}.
}
