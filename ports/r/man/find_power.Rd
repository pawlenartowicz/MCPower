% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{find_power}
\alias{find_power}
\title{Run a power simulation for all scenarios encoded in \code{contracts} (a msgpack
\code{raw} vector produced by \code{build_contract_from_spec}).}
\usage{
find_power(contracts, sample_size, n_sims, base_seed, progress)
}
\description{
\itemize{
\item \code{sample_size} — sample size to evaluate.
\item \code{n_sims}      — number of Monte-Carlo replications.
\item \code{base_seed}   — f64 in R (all R numerics are f64); cast to u64.
\item \code{progress}    — an R callback \verb{(current, total)}, or \code{NULL} for silent.
Events are delivered on the R main thread; Ctrl-C / Escape cancels the run
(see \code{progress.rs}).
}
}
\details{
Returns a named list mirroring the Python dict shape:
\verb{list(scenarios = <named list>, comparison = list())}.
}
