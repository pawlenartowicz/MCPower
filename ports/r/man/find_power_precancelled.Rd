% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{find_power_precancelled}
\alias{find_power_precancelled}
\title{Test-only seam for the cancellation path. Identical to \code{\link{find_power}} but
flips the \code{CancellationToken} \emph{before} the engine starts, so the run returns
\code{OrchestratorError::Cancelled} at its first checkpoint and \code{orchestrator_err}
maps it to the "cancelled by user" R error. Lets testthat verify the
cancel→error wiring without an interactive SIGINT (the live Ctrl-C path,
which flips the same token via \code{R_CheckUserInterrupt}, is exercised
manually). Surfaced as \code{mcpower:::find_power_precancelled}.}
\usage{
find_power_precancelled(contracts, sample_size, n_sims, base_seed)
}
\description{
Test-only seam for the cancellation path. Identical to \code{\link{find_power}} but
flips the \code{CancellationToken} \emph{before} the engine starts, so the run returns
\code{OrchestratorError::Cancelled} at its first checkpoint and \code{orchestrator_err}
maps it to the "cancelled by user" R error. Lets testthat verify the
cancel→error wiring without an interactive SIGINT (the live Ctrl-C path,
which flips the same token via \code{R_CheckUserInterrupt}, is exercised
manually). Surfaced as \code{mcpower:::find_power_precancelled}.
}
