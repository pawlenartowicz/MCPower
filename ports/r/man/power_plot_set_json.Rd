% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{power_plot_set_json}
\alias{power_plot_set_json}
\title{Ordered named list of (key → spec) pairs for a find_power result.}
\usage{
power_plot_set_json(envelope_json, show_ci, target_power_line)
}
\description{
\code{envelope_json} is produced by the R side via \code{jsonlite::toJSON(...)}.
The envelope \code{scenarios} array must contain objects with a \code{"label"} field
plus \code{power} and \code{ci} carrying the displayed arrays (R caller selects
corrected vs uncorrected before calling). Scenarios are rendered in array
order — no alphabetical sorting is applied.
}
\details{
Returns a named R list (names = block keys, values = Vega-Lite v5 JSON strings).
}
