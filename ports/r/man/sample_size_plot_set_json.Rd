% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{sample_size_plot_set_json}
\alias{sample_size_plot_set_json}
\title{Ordered named list of (key → spec) pairs for a find_sample_size result.}
\usage{
sample_size_plot_set_json(envelope_json, show_ci, target_power_line)
}
\description{
\code{envelope_json} contains \code{scenarios} as an ordered array of objects (each
with a \code{"label"} field) with one row per N point in \code{power} / \code{ci}, and
\code{histogram} carrying the corrected joint-significance histogram. Scenarios
are rendered in array order — no alphabetical sorting is applied.
Block order mirrors \code{sample_size_plot_set} (curve, at_least_k, exactly_k, …).
}
\details{
Returns a named R list (names = block keys, values = Vega-Lite v5 JSON strings).
}
