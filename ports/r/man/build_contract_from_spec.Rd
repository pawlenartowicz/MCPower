% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{build_contract_from_spec}
\alias{build_contract_from_spec}
\title{Build the whole \verb{Vec<SimulationContract>} as a single msgpack blob,
parametrising outcome_kind + estimator + intercept + clusters at the call
site.  Mirrors \code{engine-py/src/spec_builder_bridge.rs} \code{build_contract_from_spec}.}
\usage{
build_contract_from_spec(
  json,
  outcome_kind,
  link,
  estimator,
  intercept,
  clusters_json
)
}
\description{
Returns a named List with elements:
\itemize{
\item \code{names}     — character vector of scenario names
\item \code{contracts} — raw vector (msgpack-encoded \verb{Vec<SimulationContract>})
\item \code{skeleton}  — JSON string of the index-only \code{EffectSkeleton} (β-column
aligned), so the R frontend names results from it + its own
label store instead of re-deriving the factor layout.
}
}
\details{
\code{outcome_kind} is one of \code{"continuous"} | \code{"binary"} | \code{"count"}.
\code{link} is \code{"canonical"} (or \code{""}) for the canonical link, or \code{"probit"} to
override a binary outcome to the probit link.
\code{estimator}    is one of \code{"ols"} | \code{"glm"} | \code{"mle"}.
}
