# engine-wasm

Placeholder for the WASM adapter. Real implementation will bind the
`engine-orchestrator` contract via [`wasm-bindgen`](https://rustwasm.github.io/wasm-bindgen/).

The orchestrator contract (`engine-orchestrator`) is the load-bearing surface this crate binds to.
