# engine-r

Placeholder for the R adapter. Real implementation will bind the
`engine-orchestrator` contract via [`extendr`](https://extendr.github.io/).

The orchestrator contract (`engine-orchestrator`) is the load-bearing surface this crate binds to.
